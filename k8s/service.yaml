apiVersion: v1
kind: Service
metadata:
  name: central-promos-enginee
  namespace: central-promos-enginee
spec:
  type: LoadBalancer
  selector:
    app: central-promos-enginee
  ports:
    - name: http
      port: 80
      targetPort: 3000
      protocol: TCP
